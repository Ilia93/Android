<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.workapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:name=".presentation.service.notifications.NotificationService"
            android:enabled="true"
            android:exported="true"/>

        <receiver
            android:name=".presentation.service.receiver.NotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".presentation.screen.splash.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.screen.timer.timer.TimerActivity" />
        <activity android:name=".presentation.screen.archive.ArchiveActivity" />
        <activity android:name=".presentation.screen.archive.CompletedWorksInformation" />
        <activity android:name=".presentation.screen.comment.CommentActivity" />
        <activity android:name=".presentation.screen.main.MainActivity" />
    </application>

</manifest>